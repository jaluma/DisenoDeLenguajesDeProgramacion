// funciones sin return
    // sin parametros

noReturnNoParam() {
	var local:int;

	local = (a + b) * 2;
}

noReturn2Param(a:int, b:int) {
	var local:int;

	local = a + b * 2 == a || b && a;
	print cast<float>(local);
}

// funciones con return
    // sin parametros

escribeEdad(): char {
    printsp microsoft.empleados[num].edad;
    noReturn2Param(a,b);
    noReturn2Param(v[i+5], 10.2, a);
    return microsoft.empleados[num][num][num].edad;
}

    // con parametros
dobleSuma(a:int, b:int): int {
    var local:float;
    var array:[5][8] float;

    array[i+5] = array[i+6][i+7];

    local = (3 + 2) / 4 * 8;
    return cast<int>(5.6);
}

//vacia
dobleSuma(a:int, b:int): int {
    return a;
}

dobleSuma(a:int, b:int) {

}

// definicion de variables globales
var a: char;

/*
 Hipoteca.txt v1.1

- Programa de ejemplo que muestra las características del lenguaje
- La sintaxis tiene que ser obligatoriamente la que se muestra en este ejemplo

*/
//hola prueba
